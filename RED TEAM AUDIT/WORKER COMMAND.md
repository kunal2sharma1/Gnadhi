# Claude Red-Team Worker Command

Read and execute the complete task in:

`RED TEAM AUDIT/OVERNIGHT RED TEAM AUDIT PROMPT.md`

Work at full available reasoning capacity.

This is an adversarial audit. Do NOT protect the current project, thesis, architecture, workflow, or previous decisions. Treat sunk work as irrelevant.

Read the repository material required by the task packet and audit everything completed through Checkpoints 1–3.

Important execution rules:

1. Start immediately.
2. Work through the stages in order.
3. Maintain the running log exactly where the task packet specifies.
4. Save/commit the running log after every major stage so a future continuation can resume safely.
5. If your session reaches its limit, stop only after saving the current stage and writing the exact next stage to resume.
6. If I later say `CONTINUE`, read the running log and continue from the exact next unfinished stage. Do not restart completed stages.
7. Use web research when necessary to verify historical or methodological claims and preserve the source links.
8. Do not write manuscript chapters.
9. Do not redesign the project merely for stylistic preference. Recommend changes only when they improve historical accuracy, argument quality, efficiency, or reproducibility.
10. The final output must contain a blunt verdict on:
   - what is going well;
   - what is fundamentally sound;
   - what is weak;
   - what is dangerous;
   - what must change immediately;
   - what must not change;
   - the recommended operating model;
   - the recommended book direction;
   - confidence level.

Write everything into the repository.
Commit regularly.
Verify the running log exists and is current.

Do not return a large report in chat. Only return a brief status after each successful session segment:

`STAGE <number> COMPLETE — log updated — next: <stage>`

When all stages are complete:

`RED TEAM COMPLETE — final verdict recorded in running log`